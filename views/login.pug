doctype
html(lang='en')
    head
        meta(charset='utf-8')
        title MECH | Sign Up
        link(rel="stylesheet", href="./css/login.css")
        script(src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.6.0/jquery.min.js")
        script(src="https://kit.fontawesome.com/f3c7bd6f07.js" crossorigin="anonymous")
        meta(name="viewport" content="width=device-width, initial-scale=1")
        link(rel="preconnect" href="https://fonts.googleapis.com")
        link(rel="preconnect" href="https://fonts.gstatic.com" crossorigin)
        link(href="https://fonts.googleapis.com/css2?family=Exo+2:wght@200;300;400;500;600&family=Faster+One&family=Lateef:wght@200;300;400;500;600&display=swap" rel="stylesheet")

    body
        .loader 
            div 
            img(src="./img/logo-white.png", alt="logo")
        .contents
            .upper-content
                h1 MECH
                h2 تسجيل الدخول
                
                a(href="/login")
                    button تسجيل الدخول بأستخدام قوقل
                        i.fab.fa-google 
            .lower-content
                form(action="/register", method="post")
                    input(type="text", name="username", placeholder="Username")
                    input(type="password", name="password", placeholder="Password")
                    input(type="submit", value="تسجيل الدخول", id="submit")
                    a(href="/register") ليس لدي حساب 



        script(defer).
            $(window).on('load',function(){
                $('.loader').fadeOut(1000);
                $('.contents').fadeln(1000);
                });
